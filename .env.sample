###############################################
# Directus Core Configuration (SQLite example)
###############################################

DB_CLIENT=sqlite3
DB_FILENAME=/directus/database/data.db

WEBSOCKETS_ENABLED=true

# Initial admin user (only used on first boot)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin_password_change_me

# Security keys (CHANGE IN PRODUCTION)
KEY=your-secret-key-change-this-in-production
SECRET=your-secret-change-this-in-production

# CORS
CORS_ENABLED=true


###############################################
# Filter Expired Posts Hook Configuration
###############################################

# Name of the field that stores the end date
FILTER_EXPIRED_DISPLAY_FIELD=display_end_date

# Name of the status field on your collections
FILTER_EXPIRED_STATUS_FIELD=status

# Status values
FILTER_EXPIRED_STATUS_PUBLISHED=published
FILTER_EXPIRED_STATUS_DRAFT=draft

# Optional: comma-separated allow-list of collections to process.
# Leave empty to allow all collections.
# Example: posts,news,events
FILTER_EXPIRED_COLLECTIONS=

# Whether to auto-create the display_end_date field on collections.
# Accepts: "true" or "false"
FILTER_EXPIRED_AUTOCREATE_FIELD=true

# Type/interface to use when auto-creating the field
FILTER_EXPIRED_FIELD_TYPE=date
FILTER_EXPIRED_INTERFACE=display-end-date